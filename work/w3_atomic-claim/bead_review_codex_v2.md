VERDICT: FIX findings=8
| ID | sev | bead | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | high | bd-ok4pr.4.2 | The deploy runs `go install ./cmd/bd/`, which with `GOBIN` unset and `GOPATH=/home/ben/go` writes `/home/ben/go/bin/bd` (a stale 2025-07-18 binary today), never `~/.local/bin/bd` — the copy that actually resolves first on PATH — so the new acceptance line `~/.local/bin/bd version --json \| grep -q "$(git rev-parse --short HEAD)"` cannot pass and the fleet keeps running the old binary. | Replace `go install ./cmd/bd/` with `GOBIN=$HOME/.local/bin go install ./cmd/bd/` in both the Contract deploy sequence and the corresponding acceptance line. |
| 2 | high | bd-ok4pr.1.3 | The shared row scanner `scanIssues` (`internal/storage/sqlite/dependencies.go:805-850`, this bead's file) is also fed by `GetIssuesByLabel`'s fixed 26-column SELECT in `internal/storage/sqlite/labels.go:156-166`, a file in no bead's Touches, so once this bead adds the lease to the scanner every `GetIssuesByLabel` call dies with a Scan column-count mismatch and the builder cannot legally edit the file that fixes it. | Add `internal/storage/sqlite/labels.go` to this bead's Touches and name its `GetIssuesByLabel` SELECT list (:156-166) in the Contract's closure list as a required `claim_expires_at` counterpart. |
| 3 | high | bd-ok4pr.1.1 | This bead is told to add `claim_expires_at` to the explicit SELECT lists in `queries.go` (and its `pinned` grep points straight at `SearchIssues` :1644-1653), but that query's rows are scanned by `s.scanIssues` in `dependencies.go`, which the bead is explicitly forbidden to touch and sibling bd-ok4pr.1.3 only changes later, so the column-count mismatch breaks this bead's own gate `go test ./internal/storage/...`. | Add a Contract sentence excluding the SELECT lists that delegate to `s.scanIssues` (`queries.go:1644`, `ready.go:114`, `labels.go:156`) from this bead, and add `internal/storage/sqlite/queries.go` to bd-ok4pr.1.3's Touches with an explicit line requiring the `SearchIssues` list to change in the same bead as the scanner. |
| 4 | high | bd-ok4pr.1.1 | The decision ladder never rejects an empty or whitespace-only `assignee` argument — its `in_progress AND assignee == ""` row is about the stored row, not the caller's value — while bd-ok4pr.1.2's required matrix asserts `ClaimIssue` errors on that argument, so a core built strictly to this bead's text routes an empty-argument claim into the CLAIM cell and the test bead fails through no fault of its builder. | Add a Contract line above the ladder: `ClaimIssue` returns an error when the `assignee` argument is empty or whitespace-only, checked before the row is read. |
| 5 | med | bd-ok4pr.1.3 | The verbatim plan row directs the builder to `dependencies.go` ":513, :554", which are the recursive dep-tree queries whose deliberately partial column lists carry neither `pinned` nor any late column and whose scanners expect exactly those columns, so adding the lease there produces a runtime Scan error, contradicting the bead's own (correct) closure paragraph pointing at :252, :274, :827/:837 and :930/:941. | Delete ":513, :554" from the `dependencies.go` change row and cite :252, :274, :827-:837, :930-:941, adding that the recursive dep-tree queries at :512 and :553 carry no late columns and stay unchanged. |
| 6 | med | bd-ok4pr.3.1 | Its gate `go test ./cmd/bd/ -run TestClaimRoundTrip` compiles the whole `cmd/bd` package, which sibling bd-ok4pr.2.1 — same single blocker, disjoint Touches, therefore dispatched in parallel into the same worktree — is concurrently populating with a new `claim.go` and `claim_test.go`, so this bead can fail on a half-written sibling file it does not own. | Run `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import dep add bd-ok4pr.3.1 bd-ok4pr.2.1`. |
| 7 | med | bd-ok4pr.4.3 | Its acceptance includes `bash scripts/test.sh`, a whole-repo run, while the bead has no blockers and so dispatches in parallel with the storage, RPC and importer beads editing the same worktree, making the gate fail on other builders' in-flight code rather than on this one-test-file fix. | Delete the `bash scripts/test.sh` acceptance line, keeping the package-scoped `go test ./cmd/bd/doctor/fix/` runs; bd-ok4pr.4.2 already gates `go test ./...` after every dependency closes. |
| 8 | med | bd-ok4pr.4.2 | The Contract says to "record the current HEAD sha in the work report before installing", but the work report is not in this bead's Touches and no acceptance command asserts the record exists, so the prior-HEAD value the plan's rollback depends on can be silently skipped or written to an undeclared path that fails Touches validation. | Change the step to `git rev-parse HEAD > work/w3_atomic-claim/artifacts/prior_head.txt` (parent already created by the preceding `mkdir -p`) and add the acceptance line `test -s work/w3_atomic-claim/artifacts/prior_head.txt`. |
