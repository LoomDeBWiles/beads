VERDICT: FIX findings=7
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| H1 | med | plan_v3.md:83, :95, :125 | The G2 split routes `blocked`/`deferred`/`pinned`/custom statuses to the same exit 3 as a lost race while `ClaimOutcome`'s enum carries a single `denied` value (:125) and the printed `Holder` is unreliable as a discriminator (a bead moved to `blocked` keeps its old `assignee`, types.go:271-275 statuses are orthogonal to assignee), so the caller contract sold at :83 ("branch on 'lost the race' without parsing JSON") and in the before/after row at :95 ("exit 3 = someone else has it") is now false and a migrated supervisor retries a dependency-blocked or pinned bead forever instead of skipping it. | Add a `DenyReason` field (`held` vs `status`) to the `ClaimOutcome` row at :125, and reword :83 and :95 to "exit 3 = not claimable now; the reason field distinguishes contention from a non-claimable status". |
| H2 | med | plan_v3.md:83, :144 | `--assignee` is specified as required only against absence ("absent is a usage error", :83; ":144 required (usage error, exit 1, when absent)"), but cobra's required-flag check tests only that the flag was set, so `bd claim <id> --assignee ""` passes validation and matches the `in_progress ∧ assignee==""` CLAIM cell (:67) ahead of the self/RENEW cell — two such claimants both write and both exit 0, which is exactly the F1 race the required flag was added to close, and an `open` bead claimed with `""` becomes permanently re-claimable by anyone. | State at :83 and :144 that `--assignee` must be non-empty after trimming (empty value = usage error, exit 1) and add an empty-value rejection case to the `claim_test.go` matrix row at :135. |
| H3 | med | plan_v3.md:135, :146 | The G2-added "any other status → DENY exit 3" cell has no test anywhere: the storage matrix row (:135) lists open/held/self/expired/empty-assignee/closed and the CLI row (:146) lists only "exit-code matrix incl. denial output naming holder", so a builder implementing `blocked`/`deferred`/custom as exit 1 (or as a CLAIM) passes every Phase 1 and Phase 2 gate. | Add a `blocked`-status → denied-exit-3-naming-the-status case to both the `claim_test.go` matrix row (:135) and the scripttest row (:146). |
| H4 | med | plan_v3.md:153 | The row specifies adding `case "claim_expires_at"` to `checkFieldChanged` but not a comparator for `*time.Time`, and `fieldComparator` has none — `strFrom`'s type switch (importer/utils.go:31-44) returns `ok=false` for a `*time.Time`, so the nearest reusable helpers (`equalStr`/`equalPtrStr`, utils.go:65-81) report "not equal" unconditionally and `IssueDataChanged` becomes permanently true for every row whose updates map carries the key, turning every import of an otherwise-unchanged row into an UpdateIssue (updated_at bump, event insert, dirty mark, export churn); the sibling `closed_at`, also `*time.Time` and also present in all three maps, has no case for exactly this reason. | Extend the row to specify a new nil-aware `equalPtrTime` helper using `time.Time.Equal`, and have the new case call it. |
| H5 | med | plan_v3.md:154, :203 | The only importer path where the `checkFieldChanged` case is load-bearing is the external_ref match (importer.go:548-598, whose guard is `updated_at`, not hash), because the exact-hash short-circuit (:602-606) drops lease-only renewals before any map and the Phase 2 ID path (:637) is entered only when the hash already differs so status/assignee flags the change anyway — yet `TestClaimRoundTrip` as specified (:154, import into a store in pre-claim state, i.e. status differs) exercises neither, so H4's failure mode ships untested, and the Risks row (:203) states the cross-clone lag as unconditional when external_ref-bearing rows do propagate renewals. | Add a lease-only-renewal case on an external_ref-bearing issue to the `TestClaimRoundTrip` row (:154), and scope the risk row (:203) to rows without an external_ref. |
| H6 | med | plan_v3.md:133 | The multirepo row names only the INSERT copy path (:302-305, verified: `pinned` at multirepo.go:305), but the same function's else branch runs a full-column UPDATE when `content_hash` differs (multirepo.go:330-347, `pinned = ?` at :338) — and a claim always changes the hash — so a claimed issue copied onto an existing multi-repo row gets status and assignee overwritten while `claim_expires_at` keeps the target's stale value (usually NULL), leaving an `in_progress` row that is never stealable. | Extend the row to name the UPDATE branch (`multirepo.go:330-347`) alongside the INSERT copy path. |
| H7 | low | plan_v3.md:127 | The `claim.go` row still reads "DENY returns holder+expiry" without the "only when present" qualifier the G2 edit added to the Design matrix (:75-76), so a builder scoped by the change table prints an empty holder and expiry on the status-DENY path. | Mirror the Design qualifier in the row: DENY returns holder+expiry only when present, otherwise names the status. |
