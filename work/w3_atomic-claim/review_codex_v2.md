VERDICT: FIX findings=8
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| G1 | high | plan_v2.md:105-106 (Phase 1 queries.go + issues.go rows) | The F4 edit declares "The change-table rows, not the `pinned` grep, are the builder's authority" while listing only queries.go, issues.go, transaction.go and memory.go, but the `pinned` precedent it cites propagated a late-added column into explicit SELECT/INSERT column lists in `internal/storage/sqlite/ready.go:114,250`, `dependencies.go:252,274` and `multirepo.go:305` as well, so a builder scoped by the table leaves `claim_expires_at` unselected on the `bd ready`/`bd blocked`/dep-tree read paths (lease reads back nil) and unwritten in the multi-repo copy path. | Extend the Phase 1 rows to name `ready.go` (:111,:246,:399 SELECT lists + scanners), `dependencies.go` (:249,:271,:513,:554) and `multirepo.go` (:302,:335), and reword the authority note to "the table is the minimum set; the `pinned` grep enumerates the remainder". |
| G2 | med | plan_v2.md:54 vs :61 and :104 | The revised decision matrix routes every non-`open`, non-`in_progress` row to "otherwise ───► DENY (no write)" (exit 3), but the exit-code sentence at :61 and the claim.go row at :104 both put `closed`/tombstone at exit 1 error, and the DENY branch is specified to print holder+expiry which `blocked`/`deferred`/`pinned` rows (types.go:271-275) do not have — so claiming a closed bead has two contradictory exit codes and three statuses land in a denial message with nothing to print. | Replace the `otherwise` cell with two explicit cells: `closed ∨ tombstone` → ERROR (exit 1) and `blocked ∨ deferred ∨ pinned` → DENY (exit 3, message names the status, not a holder). |
| G3 | med | plan_v2.md:126 (Phase 3 importer row) | Mapping `claim_expires_at` into the two update maps is not sufficient for a lease-only change: `checkFieldChanged` has `default: return false` (internal/importer/utils.go:148-149) so `IssueDataChanged` reports no change and `UpdateIssue` is never called, and upstream the exact-content-hash branch (importer.go:602-606) already counts the row `Unchanged` because `ComputeContentHash` (types.go:67-88) excludes the new column — so a renewal (`bd claim` by the same holder, extended expiry) never propagates to a synced clone, which keeps the old expiry and can steal from a live holder. | Add `case "claim_expires_at"` in `internal/importer/utils.go` `checkFieldChanged` to the Phase 3 importer row, and state in Risks that a renewal-only change does not cross the content-hash gate so cross-clone lease views lag by one status/assignee change. |
| G4 | low | plan_v2.md:126 ("both explicit update maps") | importer.go has a third explicit update map at :370-381 inside `handleRename` (same content hash, different ID, target already present) that writes `status` and `assignee` without the lease, so that path leaves the row's `claim_expires_at` at whatever the target held while the holder changes. | Change the row to name three maps (`:370-381`, `:561-587`, `:656-682`) or state that the rename-collision map is deliberately excluded and why. |
| G5 | low | plan_v2.md:86 and :175 | Validated Assumptions row 6 still reads "Merge driver drops unmodeled fields **on conflict**" and the Risks row still scopes the change to "conflict output", both contradicting the corrected Design paragraph at :67 that the drop happens on every rewritten both-live row (`merge.go:133-141`) — the same wrong mechanism F6a was accepted to remove. | Reword both rows to "drops unmodeled fields on every row `Merge3Way` rewrites" and note the blast radius is all rewritten rows, matching :67. |
| G6 | low | plan_v2.md:187 (E2E step 4) | With `--assignee` now required (exit 1 when absent, :61), E2E step 4 still reads "`bd claim` again as agent-D" with no flag, and the deploy line at :139 says "one `bd claim` + release cycle", so a builder transcribing them literally writes a command that exits 1 and the renew case never runs. | Write step 4 as `bd claim <id> --assignee agent-D` and give the deploy line the same explicit flag. |
| G7 | low | plan_v2.md:122 (Phase 3 gate) | The pipe-free rewrite kept a runnable command for the merge half (`go test ./internal/merge/...`) but replaced the cmd/bd half with prose, "the export/import round-trip tests in `cmd/bd` pass", which names no command or test selector, so the gate is half unverifiable mechanically. | Make it `go test ./internal/merge/... ./cmd/bd/ -run TestClaimRoundTrip` (or the concrete test name the Phase 3 row adds) so the whole gate is one runnable command. |
| G8 | low | plan_v2.md:51-54 | The matrix cells are not mutually exclusive: `in_progress ∧ assignee==X ∧ lease expired` matches both the RENEW row (:52) and the STEAL row (:53), and nothing states the ladder is first-match, so the builder may emit outcome `stolen` with a steal event recording X stealing from X while the claim_test row (:109) asserts self→`renewed`. | State that the ladder is evaluated top-down first-match, so a holder whose own lease expired renews rather than steals. |
