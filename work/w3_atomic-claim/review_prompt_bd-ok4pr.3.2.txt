You are a fresh-eyes reviewer on work item w3 in the `bd` fork (Go, v0.34).
You are READ-ONLY: change no file, create no bead, commit nothing.

## What to review
Bead bd-ok4pr.3.2 — "Model assignee and lease in the JSONL merge driver".
Commit range in /home/ben/worktrees/beads/w3_atomic-claim:
`git log -p 69d9a27b9a95383793e66379bb3ba4412b13d9f2..a7e12fb35b0c58fa800efffcb329785155e7293a`

Files changed by this bead (your file set — do not go hunting elsewhere, and run
no recursive rg/grep/find outside this worktree):
- internal/merge/merge.go
- internal/merge/merge_test.go

## Judge against
1. `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import show bd-ok4pr.3.2`
   — the bead's Contract, Acceptance and Touches.
2. `work/w3_atomic-claim/plan_v4.md` — the JSONL paragraph in Design and the
   Phase 3 merge-driver rows.
3. `~/projects/shared-docs/CODING_PRINCIPLES.md`.

## Look hardest at
- The mechanism: `Merge3Way` re-marshals every rewritten both-live row from the
  modeled struct, so a field missing from `merge.Issue` is dropped on ALL
  rewritten rows, not only on conflicts. Verify the new fields survive the
  rebuild in `mergeIssue`, not merely that they exist on the struct.
- Are `assignee` and `claim_expires_at` merged as a PAIR by latest `updated_at`?
  A merge that takes assignee from one side and the lease from the other
  produces a holder whose lease belongs to someone else.
- Is the lease cleared — and the assignee kept — when the merged status is not
  `in_progress`?
- Does the test set include a no-conflict pass-through case (issue changed on
  one side in an unrelated field retains both fields)? Conflict-only tests miss
  a forgotten copy into the rebuilt result, which is the exact defect class
  here.

## Run the gate yourself
`cd /home/ben/worktrees/beads/w3_atomic-claim && go build ./... && go test ./internal/merge/...`
Report the real result. Do not modify anything to make it pass.

## Output
Write your review to:
/home/ben/worktrees/beads/w3_atomic-claim/work/w3_atomic-claim/reviews/review_bd-ok4pr.3.2_v1.md

It contains exactly this — the verdict line plus finding rows, no other prose:

VERDICT: CLEAN|FIX findings=N
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|

- CLEAN requires findings=0; FIX requires one or more finding rows.
- sev: high = wrong behavior or a broken contract; med = correctness risk in a specific case; low = wording, cosmetic.
- Each defect sentence stands alone: name the mechanism and the consequence, so the manager can disposition it with no follow-up question.
