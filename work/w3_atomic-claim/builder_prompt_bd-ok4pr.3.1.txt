You are a builder on work item w3 in the `bd` fork (Go, v0.34). Implement exactly one bead.

## Your bead
bd-ok4pr.3.1 — Carry claim lease through importer update maps and round-trip test

## Your worktree (the ONLY tree you may write to)
/home/ben/worktrees/beads/w3_atomic-claim/.runs/bd-ok4pr.3.1.1786120665171689172

Everything below runs from that directory. Do not read or write any other
worktree, and never run a recursive rg/grep/find outside it.

## Step 1 — read your inputs, in this order
1. `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import show bd-ok4pr.3.1`
   Its `## Contract` is your spec, `## Acceptance` is the exact command list you
   must make pass, `## Touches` is your write allowlist.
2. `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import show bd-ok4pr.3`
   (your epic)
3. `work/w3_atomic-claim/plan_v4.md` — the JSONL paragraph in Design, the Phase 3
   importer rows, and the "renewal-only change" risk row.
4. The already-merged claim implementation: `internal/types/types.go`,
   `internal/storage/sqlite/claim.go`.
5. `~/projects/shared-docs/CODING_PRINCIPLES.md` and
   `~/projects/shared-docs/TESTING_PRINCIPLES.md`.

## Step 2 — implement
The defect class here is silent field loss on the import-UPDATE path. Three
explicit update maps exist, and all three must carry the lease — including
`handleRename` (:370-381), which also rewrites status and assignee. A field
missing from a map is dropped with no error, leaving a synced clone
`in_progress` with no expiry, which nothing can ever steal there.

`checkFieldChanged` (utils.go, `default:` returns false) needs a
`claim_expires_at` case built on a nil-aware `*time.Time` comparator using
`time.Time.Equal`. Do not route it through the existing string helpers
(`strFrom` :31-44 rejects `*time.Time`): they would report an unconditional
change, turning every import into an UpdateIssue with an `updated_at` bump, an
event, a dirty mark, and export churn. Without the case at all, a lease-only
renewal reads as "unchanged" and never propagates.

`TestClaimRoundTrip` must cover all three paths the bead names: import into a
FRESH store (exercises `insertIssue`), import into a store ALREADY holding the
issue in its pre-claim state (exercises the update maps — this is where the drop
happens), and a lease-only renewal on an issue that carries an `external_ref`
(the one path where the `checkFieldChanged` comparator is load-bearing, because
its guard is `updated_at` rather than the content hash).

## Step 3 — gate and close, in this order
1. `bdc test <the files you changed>`
2. every `## Acceptance` command in the bead, in the bead's order, each exiting 0
3. `bdc <bead-id> <the files you changed>` — this runs the gate, makes the
   tagged pathspec commit, and closes the bead. It does not push; do not push.

The repo's gate command is already configured in the tracked `.bdc.conf` at the
worktree root. Do not create, edit, or delete that file.

## Step 4 — build report (the manager's evidence; a wrong name or field fails the audit)
Write, at this ABSOLUTE path:
/home/ben/worktrees/beads/w3_atomic-claim/work/w3_atomic-claim/builds/bd-ok4pr.3.1.1786120665171689172.build.json

one JSON object:

| Field | Value |
|---|---|
| `schema` | the number `2` |
| `bead_id` | `"bd-ok4pr.3.1"` |
| `run_id` | `"1786120665171689172"` |
| `changes` | `"committed"` when the run made a tagged commit, `"none"` when it correctly made none. No other value |
| `commits` | list of commit SHAs, no duplicates. `"committed"` → exactly the one `(bd-ok4pr.3.1)`-tagged commit `bdc` made; `"none"` → `[]` |
| `acceptance` | list, one entry per bead Acceptance command, in the bead's order, same length |

Each `acceptance` entry has exactly these four keys, no more:

| Key | Value |
|---|---|
| `criterion` | string naming what the command proves |
| `command` | the bead's Acceptance command, character for character |
| `exit` | the number `0` — a nonzero exit is a failed build, not a report |
| `log` | the entry's step log filename (grammar below) |

The acceptance step log you write is
`bd-ok4pr.3.1.1786120665171689172_step{N}.log`, where `N` is the entry's 1-based
position in `acceptance`. It is a name relative to
/home/ben/worktrees/beads/w3_atomic-claim/work/w3_atomic-claim/builds/ (no
`builds/` prefix inside the JSON), it must exist in that ABSOLUTE directory, and
it must be non-empty — a silent command records its command line and exit
status. Nothing else is accepted: `.acceptance1.log`, `.acc1.log`, `.a1.log`,
`.accept1.log` and `_step01.log` all fail.

## Worker contract
Your run worktree only. No branch switch, reset, or rebase. No push. No `wt-*`
scripts. Stay inside `## Touches`. The manager owns the rebase and the merge.
If the bead cannot be completed as written, stop and write the reason into the
build report path above instead of improvising past it.
