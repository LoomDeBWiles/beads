VERDICT: FIX findings=6

| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | high | internal/storage/sqlite/migrations_test.go:818 | `TestIssueColumnsSurviveReopen` claims in its own comment to guard migration 022's frozen column list, but it only ever exercises the ledger: I replaced 022 with the parent commit's hard-coded 28-column version and this test, `TestMigrationLedgerRecordsEveryMigration` and `TestClaimMigrationExistingStore` all still PASS, because the second `RunMigrations` skips 022 entirely, so the 295-line runtime-column rewrite that has to be right for the legacy stores where 022 genuinely does run has zero regression coverage. | Add a test that calls `MigrateDropEdgeColumns` directly against a table that still has the four edge columns plus a post-022 column carrying a value, asserting the value, the surviving indexes and the removal of the edge columns. |
| 2 | med | internal/storage/sqlite/migrations/022_drop_edge_columns.go:255 | Column names read from `pragma_table_info` are interpolated into `ALTER TABLE issues_new ADD COLUMN ...` and `INSERT INTO issues_new (...) SELECT ...` unquoted, so any column whose name needs quoting makes the migration fail and the store permanently unopenable: on a scratch store with an `"order"` column plus an edge column I got `Error: failed to open database: migration drop_edge_columns failed: ... near "order": syntax error` on every invocation, with the transaction correctly rolled back but no way forward. | Wrap every runtime-derived identifier in double quotes (`"` + name + `"`) in `addColumnClause`, `copyExpr` and the INSERT column list. |
| 3 | med | internal/storage/sqlite/store.go:158 | Skipping recorded migrations silently kills the schema self-repair that `Open` still visibly relies on, since the `RunMigrations` retry after a failed schema probe now re-runs nothing: with `pinned` dropped from a scratch store the parent binary re-added it and opened normally, while this build fails with `schema probe failed after migration retry: ... missing columns in issues: pinned. Database may be corrupted`. | Have the probe-failure path clear the ledger rows for the migrations that own the missing columns (or delete the ledger and let the backfill pass rebuild it) before retrying, so the retry can still heal drift. |
| 4 | low | internal/storage/sqlite/migrations/022_drop_edge_columns.go:52 | `canonicalIssuesColumnNames` is a hand-typed duplicate of the names inside the `canonicalIssuesColumns` DDL string, so the two can drift apart, which is the same "two lists that must agree, maintained by hand" defect this change exists to remove (drift fails loudly, as a duplicate-column or no-such-column error, rather than silently). | Drop the map and derive the known-column set by reading `pragma_table_info('issues_new')` right after the table is created. |
| 5 | low | internal/storage/sqlite/migrations.go:133 | `recordMigration` documents itself as running "inside the same EXCLUSIVE transaction as the migration itself", but it uses `db.Exec` on a pool that `store.go:135` sizes at `NumCPU()+1` for file databases, so the guarantee holds only because `Open` is single-threaded and leaves exactly one idle connection; a pooled connection swap would autocommit a ledger row for a migration that later rolls back, skipping it forever. | Run the whole migration pass on one `*sql.Conn` (or a real `*sql.Tx`) obtained once, and pass it to the migration functions, instead of relying on pool luck. |
| 6 | low | cmd/bd/migrate.go:845 | The comment "all migrations are idempotent and run on every open" is now false, and `bd migrate --inspect` still reports only registered migrations, giving no sign of which ones the ledger has recorded. | Correct the comment and add the `schema_migrations` contents to the inspection output. |
