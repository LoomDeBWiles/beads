VERDICT: FIX findings=5

| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | med | internal/storage/sqlite/schema_probe.go:23 | The probe entry for `claim_expires_at` has no test behind it: deleting the word from `expectedSchema["issues"]` and running `go test ./internal/storage/...` leaves the whole suite green (verified on a scratch copy), so the one line that turns a missed migration 027 into a startup error can silently drop out of the list again and reintroduce the runtime `GetIssue`/`SearchIssues` failure the bead closes. | Add a sqlite test that opens a store, `ALTER TABLE issues DROP COLUMN claim_expires_at`, and asserts `verifySchemaCompatibility` returns `ErrSchemaIncompatible` naming that column. |
| 2 | med | internal/storage/sqlite/schema_probe.go:18-23 | `is_template` is selected by every issue read (internal/storage/sqlite/issues.go:49, ready.go:114, queries.go:266) but is still absent from the probe's `issues` column list — verified by enumerating `pragma_table_info('issues')` against `expectedSchema`, where it is now the only unprobed column — so a store whose migration 026 never applied passes the probe and fails at runtime on every read, which is exactly the mechanism this bead closes for `claim_expires_at`, left open one column over (pre-existing, not introduced here). | Add `"is_template"` to the `issues` list alongside `"claim_expires_at"`. |
| 3 | low | internal/storage/sqlite/schema.go:37-38 | The added constant column matches migration 027 exactly (`claim_expires_at TIMESTAMP`), but nothing detects drift: removing both lines again leaves `go test ./internal/storage/...` green (verified), because `RunMigrations` always repairs the fresh database before any test looks, so the bead's assumption that "existing schema/migration tests cover item 1" is false and the next late-added column can drift the same way. | Add a test that executes the `schema` constant alone into a bare in-memory DB, with no migrations, and asserts every column in `expectedSchema["issues"]` is present. |
| 4 | low | internal/storage/memory/memory.go:496-528 | `claimEventValue` and `claimEventValues` are a verbatim second copy of internal/storage/sqlite/claim.go:14-22 and 165-183, including the steal-only `PreviousHolder` rule, so the two backends' audit payloads are held in agreement only by hand and can diverge silently — the same constant-versus-implementation drift class this bead exists to fix. | Move the struct and the render function to one shared location (`internal/storage` or `internal/types`) and have both `recordClaimEvent` and `MemoryStorage.ClaimIssue` call it. |
| 5 | low | internal/storage/memory/memory.go:435-441 | The new case accepts only `*time.Time` and an interface nil and silently ignores anything else, so a caller passing a `time.Time` by value — a type the sqlite path admits through `allowedUpdateFields` (queries.go:578) and hands straight to the driver — has its lease dropped on the memory backend with no error, the same silent-drop failure mode the case was added to remove. | Also accept `time.Time` (taking its address) in the type switch, or return an error on an unrecognized value type instead of falling through silently. |
