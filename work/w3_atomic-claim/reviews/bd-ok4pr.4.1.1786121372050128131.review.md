VERDICT: FIX findings=3
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | low | CODEMAP.md:53 | The migration-runner row still reads "(26 migrations)" while `internal/storage/sqlite/migrations.go:15-46` now registers 27 entries because this work item added `claim_expires_at_column`, so the codemap states a count the code contradicts in the very commit meant to bring it current. | Change the row to "Schema migration runner (27 migrations)". |
| 2 | low | CONTEXT.md:41 | The note says `update` "reads the issue, decides in Go, then writes ... so two agents ... both decide 'free'", which implies `UpdateIssue` evaluates ownership and loses a race, when in fact it applies an unconditional `UPDATE ... WHERE id = ?` with no ownership check at all (queries.go:757-778), so a reader may look for a check to harden rather than understanding there is none. | Reword to say the caller decides "free" from a prior `bd show`/read while `update` itself writes unconditionally with no precondition, so both writes land. |
| 3 | low | CONTEXT.md:41 | The note says renewal happens by claiming an issue you already hold but omits that `writeClaim` sets `claim_expires_at` from this call alone (internal/storage/sqlite/claim.go:130-136), so a renewal issued without `--lease` clears the expiry and silently converts a leased claim into a never-stealable one, defeating the advice to always pass `--lease`. | Add one clause: a renewal resets the lease from the current invocation, so every renewal must repeat `--lease`. |
