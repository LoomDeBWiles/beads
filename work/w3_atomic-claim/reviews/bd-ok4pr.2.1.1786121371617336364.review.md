VERDICT: FIX findings=5

| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| F1 | high | cmd/bd/scripttest_test.go:1 | The scripttest runner is behind `//go:build scripttests`, so the bead's stated gate `go test ./cmd/bd/` never executes `testdata/claim.txt` (verified: `go test ./cmd/bd/ -run Script` reports "no tests to run"), which is exactly the illusory-coverage failure the bead was written to prevent — the file is at the correct flat path and passes when the tag is supplied, but nothing in the acceptance command runs it. | Either add `-tags scripttests` to the acceptance/CI command for this package or move the claim exit-code assertions into a tag-free Go test in `cmd/bd/` that shells out to the built binary. |
| F2 | med | cmd/bd/testdata/claim.txt:61 | The daemon half of the script asserts only that a daemon is running once at line 62, and `bd` silently falls back to direct mode with a "Daemon took too long to start (>5s). Running in direct mode." warning (observed on `dep_add` in the same suite run), so a broken `handleClaim` could pass this file by re-running the direct path under a daemon-mode heading. | Assert the RPC route was taken for at least one daemon-mode claim, e.g. capture stderr and require the absence of the direct-mode fallback warning, or check the daemon log for the `claim` operation. |
| F3 | low | cmd/bd/claim.go:136 | `claimRPCArgs` converts the lease with `int64(lease.Seconds())`, truncating sub-second remainders, while the direct path passes the exact `time.Duration` to `store.ClaimIssue`, so `--lease 90s500ms` produces a different expiry depending on whether a daemon happens to be running. | Truncate the duration to whole seconds once in `claimFlags` so both routes send the same value, or widen the wire field to milliseconds. |
| F4 | low | internal/rpc/server_issues_epics.go:519 | The handler accepts any `LeaseSeconds` including zero or negative and converts it straight to a duration, producing an already-expired claim that any rival steals immediately, whereas the CLI rejects anything under 1s at cmd/bd/claim.go:78 — the two entry points enforce different lease rules. | Reject `*claimArgs.LeaseSeconds < 1` in `handleClaim` with the same error the CLI raises. |
| F5 | low | cmd/bd/testdata/claim.txt:1 | `go test -tags scripttests ./cmd/bd/` — the only command that runs this script — exits non-zero on pre-existing `help`, `init` and `dep_add` script failures (claim itself passes), so the claim script cannot be gated green without first clearing that baseline. | Fix or quarantine the three stale scripts (`help.txt:4` expects the pre-grouping "Available Commands:" header) so the tagged suite has a zero baseline before wiring it into the gate. |
