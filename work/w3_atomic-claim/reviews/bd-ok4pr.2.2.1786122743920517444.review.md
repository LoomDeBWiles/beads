VERDICT: FIX findings=3

| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| F1 | high | cmd/bd/claim_test.go:355,414 | The denial assertion compares the observed exit status against the package's own `claimDeniedExit` constant instead of the literal 3, so it is tautological: changing `cmd/bd/claim.go:22` to `const claimDeniedExit = 1` leaves the whole gate green (`go test ./cmd/bd/ ./internal/rpc/...` → ok, verified by mutation), meaning the exit-3 half of the contract item 1 exists to enforce — the value that lets a shell caller tell a lost race from a broken command — is still unasserted in the default gate. | Assert the literal in both modes (`if denied.exit != 3`), keeping the constant only in the message, so the documented exit code cannot be redefined out from under the test. |
| F2 | med | cmd/bd/claim_test.go:237 | `claimTestBinary` builds the 33 MB `bd` binary into an `os.MkdirTemp` directory that is never removed by any cleanup, so every `go test ./cmd/bd/` run permanently leaks 33 MB into `/tmp` (23 such directories, ~760 MB, were present in this worktree before this review trashed them). | Register the removal once — build inside a `TestMain` that `defer os.RemoveAll(dir)`s it, or have the `sync.Once` record the dir and delete it from `TestMain` after `m.Run()`. |
| F3 | low | cmd/bd/claim_test.go:399 | The `strings.Contains(res.stderr, "direct mode")` guard only catches the auto-start-timeout warning (`cmd/bd/daemon_autostart.go:309`, unconditional); the connect-failed and health-failed fallbacks print through `emitVerboseWarning` (`daemon_autostart.go:411`) which is gated on `BD_VERBOSE` being set, and the test does not set it, so the comment overstates what the guard proves and the real proof of the RPC route rests entirely on `assertDaemonServedClaims`. | Set `BD_VERBOSE=1` in the daemon test's env so every fallback reason is actually printed and caught, and reword the comment to say the metrics check is the primary proof. |
