# Plan Delta v2 — dispositions of review_codex_v1.md (Claude fallback lane)

All six findings ACCEPT. Bindings target plan_v1.md section bytes (checker-canonical paths).

| Finding ID | Disposition | Heading Path | Prior SHA-256 | Instruction |
|---|---|---|---|---|
| F1a | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Design | c519270e5a3b5ad8eb867db1d7c7c89fe721eb332277d349746561f6cbd56e65 | In the command sketch mark `--assignee X (required)`; after the exit-codes sentence, state: absent `--assignee` is a usage error (exit 1) — a defaulted actor makes two defaulted claimants collide in the RENEW cell and both exit 0. |
| F1b | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes > Phase 2: RPC + CLI — Gate: `go test ./cmd/bd/ ./internal/rpc/...` passes; scripttest covers exit 0/3/1 in both daemon and direct modes | 9cc74cddd1d847fff2630aa0043c0142af539504546beedeabe78cd7fff32149 | In the `cmd/bd/claim.go` row replace the actor-default clause with: `--assignee` required (usage error, exit 1, when absent; no default). |
| F2a | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes | 4e5c47bd44ea983062ad56179810a017bf8d14ae49a7c8ac92b2ff6db6672bbd | Add row for `internal/importer/importer.go` mapping `claim_expires_at` in both explicit update maps (:561-587, :656-682); extend the round-trip row to import into an existing store (update path), not only a fresh one. |
| F2b | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Files NOT Affected (verified) | 89c324dcdb5eec38335de34eec31fd6fbae4aa468cc4619fb909919678bf950f | Delete the `internal/importer/importer.go` row (file is now affected in Phase 3). |
| F2c | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Verification | 38600ca39193ab422740f09eea2615fea53e02d45cda2f6526eac1c0b84cd144 | E2E item 6: export → import into a fresh store AND into a store already holding the issue (pre-claim state) → lease identical in both. |
| F2d | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes > Phase 1: Storage + migration — Gate: `go test ./internal/storage/... ./internal/types/...` passes; new race test proves exactly one winner | 2f61cd0bbc1b102fa2ec3e8a1e577a707d6b07b221e8491a5ce8d885e012e58a | In the queries.go row add `claim_expires_at` to `allowedUpdateFields` (:545, import path only — no CLI flag). |
| F3 | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes > Phase 1: Storage + migration — Gate: `go test ./internal/storage/... ./internal/types/...` passes; new race test proves exactly one winner | 2f61cd0bbc1b102fa2ec3e8a1e577a707d6b07b221e8491a5ce8d885e012e58a | Add row: `internal/storage/memory/memory.go` implements `ClaimIssue` (same matrix, existing mutex; backs `--no-db`, `cmd/bd/nodb.go:44`) — interface change otherwise breaks compilation. |
| F4 | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes > Phase 1: Storage + migration — Gate: `go test ./internal/storage/... ./internal/types/...` passes; new race test proves exactly one winner | 2f61cd0bbc1b102fa2ec3e8a1e577a707d6b07b221e8491a5ce8d885e012e58a | Add row: `internal/storage/sqlite/issues.go` — `claim_expires_at` in both explicit INSERT column lists (`insertIssue` :44, `insertIssues` :74); note the change-table rows, not the `pinned` grep, are the builder's authority. |
| F5a | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Design | c519270e5a3b5ad8eb867db1d7c7c89fe721eb332277d349746561f6cbd56e65 | Add matrix cell `in_progress ∧ assignee==""` → CLAIM (legacy `update --status in_progress` rows have no owner; denying makes them permanently unclaimable during migration). |
| F5b | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes > Phase 1: Storage + migration — Gate: `go test ./internal/storage/... ./internal/types/...` passes; new race test proves exactly one winner | 2f61cd0bbc1b102fa2ec3e8a1e577a707d6b07b221e8491a5ce8d885e012e58a | Add empty-assignee-in_progress → claimed to the `claim_test.go` outcome matrix. |
| F6a | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Design | c519270e5a3b5ad8eb867db1d7c7c89fe721eb332277d349746561f6cbd56e65 | Correct the merge paragraph: `Merge3Way` re-marshals every rewritten both-live row from the modeled struct (merge.go:133-141) — unmodeled fields drop on all rewritten rows, not only true conflicts. |
| F6b | ACCEPT | w3_atomic-claim: Atomic claim verb with owner lease for bd > Changes | 4e5c47bd44ea983062ad56179810a017bf8d14ae49a7c8ac92b2ff6db6672bbd | Add no-conflict pass-through case to the `merge_test.go` row: one-sided unrelated-field change retains `assignee` and `claim_expires_at`. This Changes binding also authorizes rewording Phase 3's gate heading to drop its raw pipe characters, unrepresentable in this table. |
