VERDICT: FIX findings=9
| ID | sev | bead | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | high | bd-ok4pr.3.1 | The bead has no `blocks` edge from bd-ok4pr.1.1 yet its `TestClaimRoundTrip` must create a claimed issue carrying a lease, which needs `types.Issue.ClaimExpiresAt`, migration 027 and `ClaimIssue` — all introduced by bd-ok4pr.1.1 — so a parallel dispatch gives a builder a package that cannot compile. | Run `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import dep add bd-ok4pr.3.1 bd-ok4pr.1.1`. |
| 2 | med | bd-ok4pr.2.1 | Contract and Touches place the new scripttest files in `cmd/bd/testdata/script/*.txt`, but the runner globs `testdata/*.txt` (`cmd/bd/scripttest_test.go:43`), so scripts written to a `script/` subdirectory never execute and the `go test ./cmd/bd/` gate passes without testing exit 0/3/1 at all. | Replace both the Contract sentence and the Touches line with `cmd/bd/testdata/*.txt`, stating that scripttest files live directly in `cmd/bd/testdata/`. |
| 3 | med | bd-ok4pr.1.1 | The acceptance line `grep -q 'ClaimIssue' internal/storage/storage.go internal/storage/sqlite/claim.go internal/storage/memory/memory.go internal/storage/sqlite/transaction.go` exits 0 as soon as the first file matches, so the bead passes with the memory backend and the tx twin unimplemented. | Rewrite it as the per-file loop already used in this bead's column check: `for f in internal/storage/storage.go internal/storage/sqlite/claim.go internal/storage/memory/memory.go internal/storage/sqlite/transaction.go; do grep -q ClaimIssue "$f" || exit 1; done`. |
| 4 | med | bd-ok4pr.2.1 | The acceptance line `grep -q 'OpClaim' internal/rpc/protocol.go internal/rpc/client.go internal/rpc/server_issues_epics.go` likewise short-circuits on the first match, so the bead passes with the RPC handler or the client call missing entirely. | Replace with a per-file loop asserting the right symbol in each file (`OpClaim` in protocol.go, `func .*Claim(` in client.go, `OpClaim` case in server_issues_epics.go). |
| 5 | med | bd-ok4pr.1.2 | `TestClaimMigrationExistingStore` requires a pre-027 fixture database, but Touches lists only `internal/storage/sqlite/claim_test.go` and no `internal/storage/sqlite/testdata/` directory exists in the repo, so committing a fixture file fails Touches validation and escalates. | Add a Contract sentence requiring the pre-027 schema to be built in-code inside the test (no committed fixture), or add `internal/storage/sqlite/testdata/**` to Touches. |
| 6 | med | bd-ok4pr.4.2 | Acceptance asserts `test -s work/w3_atomic-claim/artifacts/e2e_claim_output.txt` but no acceptance command writes that file — both E2E runs pipe into `tail`/`grep` — so the builder must infer a `tee` and the artifact gate fails on a correct implementation. | Replace the two E2E acceptance lines with one: `mkdir -p work/w3_atomic-claim/artifacts && bash work/w3_atomic-claim/e2e_claim.sh | tee work/w3_atomic-claim/artifacts/e2e_claim_output.txt | tail -1 | grep -q E2E_PASS`, keeping a following `grep -q 'one exit=0, one exit=3' work/w3_atomic-claim/artifacts/e2e_claim_output.txt`. |
| 7 | med | bd-ok4pr.4.2 | The deployed-identity acceptance is bare `~/.local/bin/bd version`, which exits 0 against the pre-existing v0.34 binary, so a silently failed `go install` still passes the gate the plan defines as "installed bd version reports new HEAD". | Change the line to `~/.local/bin/bd version --json | grep -q "$(git rev-parse --short HEAD)"` (version.go:36-45 emits a `commit` field). |
| 8 | med | bd-ok4pr.4.2 | The bead runs `bd daemon --stop-all`, which terminates all six live daemons on this shared host including workspaces owned by other agents, and the bead gives no record-then-verify step, so a mid-flight daemon-backed operation elsewhere can be cut. | Add a Contract step to record `bd daemons list --json` output into the artifact before stopping and to re-check `bd daemons health --json` after, and state explicitly that daemons autostart on next invocation. |
| 9 | low | bd-ok4pr.1.1 | The bead spans 12 files across migration, type model, storage interface, two backends and four read-path files, and its title carries an "and" — over the BEADS.md single-responsibility sizing test — so one builder session carries the whole storage layer. | Split the late-column read-path plumbing (`ready.go`, `dependencies.go`, `multirepo.go`) into a second bead blocked by the core, and retitle the core "Add atomic ClaimIssue storage core". |
