You are a builder on work item w3 in the `bd` fork (Go, v0.34). Implement exactly one bead.

## Your bead
bd-ok4pr.1.2 — Test claim decision matrix and concurrent-claim race

## Your worktree (the ONLY tree you may write to)
/home/ben/worktrees/beads/w3_atomic-claim/.runs/bd-ok4pr.1.2.1786119304126143397

Everything below runs from that directory. Do not read or write any other
worktree, and never run a recursive rg/grep/find outside it.

## Step 1 — read your inputs, in this order
1. `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import show bd-ok4pr.1.2`
   Its `## Contract` is your spec, `## Acceptance` is the exact command list you
   must make pass, `## Touches` is your write allowlist — writing outside it
   fails the gate.
2. `bd --db /home/ben/projects/beads/.beads/beads.db --no-auto-import show bd-ok4pr.1`
   (your epic)
3. `work/w3_atomic-claim/plan_v4.md` — the Design decision ladder is the
   authority on every cell you assert.
4. The already-merged implementation you are testing:
   `internal/storage/sqlite/claim.go`, `internal/storage/memory/memory.go`,
   `internal/types/types.go`.
5. `~/projects/shared-docs/TESTING_PRINCIPLES.md` and
   `~/projects/shared-docs/CODING_PRINCIPLES.md`.

## Step 2 — implement
Your job is to prove the claim contract, not to describe it. Two rules:
- Tests assert the plan's semantics, NOT whatever the current implementation
  happens to do. If a case in the Design ladder disagrees with the code, that is
  a finding: leave the test asserting the plan and say so in your build report.
  Do not edit the implementation — it is outside your Touches.
- The race test is the point of the whole item: N=8 goroutines claim one open
  issue; assert EXACTLY one outcome is `claimed`, seven are denied, and the
  final stored assignee is the winner. A test that merely checks "no error" or
  that passes with a non-atomic implementation is worthless here. It must run
  clean under `-count=5`.

Also required by the bead: a denial writes nothing (no `updated_at` bump, no
event row, no dirty mark) and a pre-027 store migrates cleanly with its data
intact — build that pre-027 schema in code, never as a committed fixture DB.

## Step 3 — gate and close, in this order
1. `bdc test <the files you changed>`
2. every `## Acceptance` command in the bead, in the bead's order, each exiting 0
3. `bdc <bead-id> <the files you changed>` — this runs the gate, makes the
   tagged pathspec commit, and closes the bead. It does not push; do not push.

The repo's gate command is already configured in the tracked `.bdc.conf` at the
worktree root. Do not create, edit, or delete that file.

## Step 4 — build report (the manager's evidence; a wrong name or field fails the audit)
Write, at this ABSOLUTE path:
/home/ben/worktrees/beads/w3_atomic-claim/work/w3_atomic-claim/builds/bd-ok4pr.1.2.1786119304126143397.build.json

one JSON object:

| Field | Value |
|---|---|
| `schema` | the number `2` |
| `bead_id` | `"bd-ok4pr.1.2"` |
| `run_id` | `"1786119304126143397"` |
| `changes` | `"committed"` when the run made a tagged commit, `"none"` when it correctly made none. No other value |
| `commits` | list of commit SHAs, no duplicates. `"committed"` → exactly the one `(bd-ok4pr.1.2)`-tagged commit `bdc` made; `"none"` → `[]` |
| `acceptance` | list, one entry per bead Acceptance command, in the bead's order, same length |

Each `acceptance` entry has exactly these four keys, no more:

| Key | Value |
|---|---|
| `criterion` | string naming what the command proves |
| `command` | the bead's Acceptance command, character for character |
| `exit` | the number `0` — a nonzero exit is a failed build, not a report |
| `log` | the entry's step log filename (grammar below) |

The acceptance step log you write is
`bd-ok4pr.1.2.1786119304126143397_step{N}.log`, where `N` is the entry's 1-based
position in `acceptance`. It is a name relative to
/home/ben/worktrees/beads/w3_atomic-claim/work/w3_atomic-claim/builds/ (no
`builds/` prefix inside the JSON), it must exist in that ABSOLUTE directory, and
it must be non-empty — a silent command records its command line and exit
status. Nothing else is accepted: `.acceptance1.log`, `.acc1.log`, `.a1.log`,
`.accept1.log` and `_step01.log` all fail.

## Worker contract
Your run worktree only. No branch switch, reset, or rebase. No push. No `wt-*`
scripts. Stay inside `## Touches`. The manager owns the rebase and the merge.
If the bead cannot be completed as written, stop and write the reason into the
build report path above instead of improvising past it.
