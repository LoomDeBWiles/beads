VERDICT: FIX findings=4

| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | med | internal/storage/sqlite/schema_probe.go:17-23 | `expectedSchema["issues"]` still ends at `pinned` and does not list `claim_expires_at`, so a store where migration 027 failed to apply passes the open-time probe that exists to trigger the migration retry (store.go:158-172) and instead fails at runtime on every `GetIssue`/`SearchIssues`, which now select the column, turning a diagnosable startup error into a total read-path failure. | Add `"claim_expires_at"` to the `issues` column list in `expectedSchema` (file is outside this bead's Touches, so disposition as a follow-up bead). |
| 2 | med | internal/storage/memory/memory.go:377-460 | `MemoryStorage.UpdateIssue`'s key switch has no `claim_expires_at` case while the sqlite backend admits that key through `allowedUpdateFields` (queries.go:575), so in `--no-db` mode any update carrying a lease (the JSONL import path that bd-ok4pr.3.1 builds on) silently drops it and leaves the row `in_progress` with no expiry, permanently unstealable on that backend. | Add a `case "claim_expires_at"` to memory's update switch accepting `*time.Time` and `nil`, mirroring the sqlite column write. |
| 3 | low | internal/storage/memory/memory.go:551-556 | The memory backend's claim event is written with nil `OldValue`/`NewValue`, so a steal there records no `previous_holder` while the sqlite event does (claim.go:170-176), leaving a `--no-db` steal unreconstructable from the audit trail; this matches memory's existing convention of payload-free events but not the claim contract's "steal event carries the prior holder". | Marshal the same `claimEventValue` payload into `Event.NewValue` in memory's `ClaimIssue`, or record in the bead that memory audits are payload-free by design. |
| 4 | low | internal/storage/sqlite/schema.go:5-39 | The `issues` CREATE TABLE omits `claim_expires_at` although `pinned` and `is_template` (the same late-added-column precedent, migrations 023 and 026) are present in both the schema constant and their migration, so a fresh database gets the column only because `RunMigrations` happens to run immediately after `db.Exec(schema)` at store.go:153-158 and any future path that applies the schema without migrations reads back "no such column". | Add `claim_expires_at TIMESTAMP` to the schema constant alongside the migration, as `pinned` does (file is outside this bead's Touches, so disposition as a follow-up bead). |
