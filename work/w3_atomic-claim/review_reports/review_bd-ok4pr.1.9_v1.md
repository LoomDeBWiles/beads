VERDICT: FIX findings=3
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| 1 | med | internal/storage/sqlite/migrations/022_drop_edge_columns.go:297-304 | The rebuild replays surviving CREATE INDEX statements but nothing replays triggers or user views, so `DROP TABLE issues` silently destroys any trigger or non-bd view on `issues` during the one backfill pass every ledgerless store takes — reproduced on a store built by the pre-item binary, where a trigger on `issues` was gone after a single `bd list` with this build while a plain index survived. | Read `sqlite_master` for `type IN ('trigger','view')` with `tbl_name='issues'` alongside the index read, filter with the same `mentionsEdgeColumn` test, replay after the rename, and assert trigger survival in `TestDropEdgeColumnsRebuildKeepsLaterColumns`. |
| 2 | low | internal/storage/sqlite/migrations.go:143 | `AppliedMigrations` orders by `applied_at, name` while `applied_at` defaults to `CURRENT_TIMESTAMP` at one-second granularity (schema.go:259), so a backfill stamps all 27 rows identically and `bd migrate --inspect` prints them alphabetically under a heading that reads as application order. | Order by position in `migrationsList`, or add a monotonic sequence column to the ledger. |
| 3 | low | internal/storage/sqlite/migrations.go:141 and :164 | `AppliedMigrations` and `appliedMigrations` are two hand-maintained readers of the same ledger table with duplicated queries and error wording, differing only in return shape — the duplicate-list burden item 4 of this same bead deleted from migration 022. | Keep the exported `AppliedMigrations` as the single reader and derive the internal name-set from its result. |
