VERDICT: FIX findings=11
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| R1 | high | plan_v1.md:73 | Change A hashes only when `mtime > last_import_time`, so different JSONL content with an older or equal mtime passes as fresh even though `cmd/bd/integrity.go:118-121` explicitly removed this unsafe shortcut. | Make `CheckStaleness` compare the JSONL SHA-256 whenever stored hash metadata exists, add the older-or-equal-mtime divergence case, and benchmark the 811,285-byte fleet file plus a large fixture. |
| R2 | high | plan_v1.md:63 | Overwriting `jsonl_content_hash` before rename means a crash or rename failure leaves the old visible JSONL against the new hash, after which `validatePreExport` rejects the mismatch instead of letting the next normal export heal it. | Specify a committed-plus-pending hash protocol that retains the old committed hash, records the new pending hash, renames, then promotes the pending hash, with readers accepting exactly the committed or pending content. |
| R3 | high | plan_v1.md:80 | Continuing to rename after a metadata write failure can publish healthy new bytes with no matching recorded hash, so a later direct freshness check can still return the false stale error. | Abort publication when the pending hash cannot be recorded and make only post-publication timestamp bookkeeping best-effort. |
| R4 | high | plan_v1.md:96 | Change C covers only `exportToJSONLWithStore`, while `autoflush.go:431-749` and `sync.go:1408-1444` reveal before metadata and RPC writers at `server_export_import_auto.go:189,560` never record the staleness hash, so healthy rewrites retain the same false-stale class. | Route every default `issues.jsonl` rewrite, including direct autoflush, sync, manual and RPC export, and import-triggered export, through one shared internal publisher with the same hash protocol. |
| R5 | high | plan_v1.md:80 | `operationMu` is process-local, so daemon export and direct autoflush can interleave metadata writes and atomic renames until the final file and hash belong to different snapshots. | Add a dedicated cross-process publication lock held from the database snapshot through metadata promotion and conditional dirty clearing by every JSONL writer. |
| R6 | high | plan_v1.md:81 | Clearing the pre-read dirty IDs after export can delete a newer mark for the same issue because `ClearDirtyIssuesByID` deletes solely by ID, silently stranding a mutation made after that issue was serialized. | Snapshot each dirty row's `marked_at` value and delete it only if that value is unchanged, then add a barrier-based test proving a mutation during export remains dirty. |
| R7 | med | plan_v1.md:82 | The planned tests inspect only final hash and dirty count, so rename-before-metadata ordering, crash and metadata failure states, and a reader inside the publication window can all remain broken while the tests pass. | Add deterministic tests with barriers around metadata and rename plus injected metadata and rename failures, asserting both `CheckStaleness` and `validatePreExport` before, during, and after publication. |
| R8 | med | plan_v1.md:145 | Checks 5 and 7 omit `--no-daemon`, so `main.go` connects to the healthy daemon even with `--db` and `--no-auto-import`, and `info.go` skips `CheckStaleness`, making check 5 return zero on the current broken binary. | Add `--no-daemon` to both checks and invoke the exact built or installed binary whose behavior is under test. |
| R9 | med | plan_v1.md:135 | The E2E's unspecified direct mutation can take the ordinary export path, while check 6's broad JSONL log count does not prove the pre-import dirty flush clears once, so Change B can remain broken while the E2E passes. | In the scratch repo, mutate with `--no-daemon --no-auto-flush`, trigger the watcher with a same-bytes atomic JSONL replacement, assert one pre-import flush and zero dirty rows, trigger it again, and assert no second flush. |
| R10 | med | plan_v1.md:131 | The baseline command targets the currently nonexistent `artifacts/` directory and the post-change gate provides no command that extracts comparable failure lists, so Phase 2 has no reproducible no-new-failures decision. | Create the artifacts directory first and specify one script that records command status and normalized failing test names for baseline and post-change runs before comparing them. |
| R11 | med | plan_v1.md:115 | Rollback copies over the installed executable before stopping processes that execute it, which can fail with `ETXTBSY` and prevents the claimed seconds-long full undo. | Use the backup binary to stop all daemons, copy the backup to a new temporary path, atomically rename it over `~/.local/bin/bd`, and verify the restored version. |
