VERDICT: FIX findings=12
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| V2-1 | high | plan_v2.md:71-95 | `ContentState` hashes the file and then reads metadata without locking or retrying, so a reader that samples file A before an A-to-B publish and committed B after it reports a healthy repository diverged. | On a provisional mismatch, acquire `.beads/.publish.lock`, recompute the file hash and both metadata keys, and return diverged only if the locked recheck still mismatches. |
| V2-2 | high | plan_v2.md:73-84 | `Publish` accepts `issues` and `dirtySnapshot` that callers construct before entering it, so publishers can snapshot A then B, acquire the lock in B/A order, and leave stale A as the final file after B's dirty row was cleared. | Change `Publish` to accept a snapshot-builder callback that reads the existing JSONL, database issues, and dirty rows only after acquiring the publish lock, then conditionally clears that same snapshot before releasing it. |
| V2-3 | high | plan_v2.md:71,118 | Specifying `hasJSONLChanged` as `changed = diverged` maps `no-metadata` to unchanged even though `integrity.go:134-136` currently returns true, allowing an existing unrecorded JSONL to skip import and pre-export refusal. | Preserve caller-specific semantics by mapping `no-metadata` to fresh in `CheckStaleness` but changed in `hasJSONLChanged` when the JSONL exists, with tests for both callers. |
| V2-4 | high | plan_v2.md:115-123 | Phase 2 never records metadata after `importToJSONLWithStore`, so daemon auto-import and daemon sync at `daemon_sync.go:633,854` rewrite SQLite but leave the pulled file hash uncommitted and the next freshness gate still fails. | Add a suffix-aware import-record operation and invoke it after every successful `importToJSONLWithStore` path, including imports that change zero rows. |
| V2-5 | high | plan_v2.md:78-95 | Only `Publish` deletes `jsonl_pending_hash`, so a crash after pending B followed by successful import X leaves B accepted indefinitely, while `AutoImportIfNewer` can also run its post-import export callback before committing X at `autoimport.go:131-137`. | Add a publish-lock-protected `RecordImport` that commits the imported hash and deletes pending before any post-import export callback, then route all CLI, daemon, and RPC import metadata writers through it. |
| V2-6 | high | plan_v2.md:61,97-122 | The claimed five-writer inventory omits the direct manual writer at `cmd/bd/export.go:472`, so `bd export -o .beads/issues.jsonl` still renames and clears dirty rows outside the lock and two-key protocol. | Route only the default-path branch of `cmd/bd/export.go` through `Publish`, while leaving stdout and custom-path exports as non-metadata copies. |
| V2-7 | med | plan_v2.md:97,122 | Replacing `handleExport` wholesale with `Publish` makes any RPC export to a custom `ExportArgs.JSONLPath` overwrite the default repository's committed hash and dirty state. | Resolve the canonical default JSONL from the server store and use `Publish` only when the requested path matches it, retaining a plain atomic writer for custom exports. |
| V2-8 | med | plan_v2.md:109-110 | Adding dirty-snapshot methods to `storage.Storage` only in SQLite leaves `internal/storage/memory.MemoryStorage`, whose dirty map is boolean, unable to satisfy the interface and breaks the build. | Define a narrow publisher-store interface containing the new snapshot methods and require it only on DB-backed publish paths instead of expanding the shared `storage.Storage` interface. |
| V2-9 | med | plan_v2.md:111,191 | The state-machine tests stop at rename failure, where the old file merely matches committed, and never test a reader across a successful rename or failures after each promotion sub-write. | Add deterministic barriers and failpoints after pending, successful rename, committed write, file-hash write, timestamp write, pending deletion, and conditional clear, asserting the real `ContentState`, `CheckStaleness`, and pre-export behavior at each state. |
| V2-10 | med | plan_v2.md:105,113,176-178,195 | The subset gate records only lines beginning `--- FAIL:`, so a compile or package-setup failure can produce an empty post-change set that incorrectly passes as a subset, and no runnable comparison command is specified. | Capture `go test -json` output and command status for baseline and post-change runs, normalize both test failures and package build failures, and fail with an explicit `comm -13` comparison when any new signature appears. |
| V2-11 | med | plan_v2.md:180-185 | The E2E is not executable as written because `$BD init`, daemon startup, same-bytes replacement, assertions, log baselining, scratch paths, and teardown are prose or placeholders rather than commands. | Replace the five steps with one checked scratch-repo script using `init --quiet`, explicit database and log paths, exact atomic replacements and SQL assertions, and cleanup of the recorded daemon process. |
| V2-12 | med | plan_v2.md:125-133 | Deploy stops all seven running daemons but only says they will relaunch on demand, which does not satisfy the settled requirement to restart the daemons that were running before deployment. | Record each running daemon's workspace and launch options before `--stop-all`, relaunch that exact set after installation, and verify each reports healthy on the new binary. |
