VERDICT: FIX findings=10
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| R4-1 | high | plan_v4.md:117 | The locked divergence guard runs before snapshot construction and temp-file writing, so a git checkout or manual write that ignores the publish lock can replace the canonical file after the guard and still be silently overwritten at step 6. | Capture the guarded file hash and recheck it under the publish lock immediately before rename, aborting with a typed divergence error if the canonical bytes changed. |
| R4-2 | high | plan_v4.md:177 | When the daemon sees a divergent file while dirty rows exist, its pre-import `Publish` returns an error and the existing `performAutoImport` path returns before import, so every retry aborts at the same flush and the import that would heal divergence is starved. | Specify a typed divergence branch that skips the pre-import flush, imports and records the canonical bytes, then publishes the still-dirty snapshot, and add this exact dirty-plus-diverged watcher regression. |
| R4-3 | high | plan_v4.md:118 | `Publish` aborts only on `diverged`, so an existing canonical JSONL with neither hash key can be overwritten even though the preserved `hasJSONLChanged` mapping correctly treats that state as requiring import first. | Make the writer guard also abort on `no-metadata` when the canonical file exists, allowing `no-metadata` publication only when the file is absent. |
| R4-4 | high | plan_v4.md:116 | Step 2 invokes `ContentState` while holding the publish lock even though its provisional-mismatch path acquires that lock, and the existing `internal/lockfile` API exposes only an uninterruptible blocking acquisition, leaving the divergence path self-deadlocking with no implementable context-aware API in the change list. | Add a context-aware API to `internal/lockfile`, add a lock-held content-state sampler that never reacquires the lock, list both changes in Phase 1, and test cancellation plus locked divergence. |
| R4-5 | med | plan_v4.md:175 | The CLI import row says to replace the metadata block at lines 388-396, but the canonical post-import flush already runs at lines 374-379, so a builder following the row calls `Publish` before `RecordImport` and the new guard aborts required ID-remapping exports. | Move canonical `RecordImport` immediately after the successful import and before `flushToJSONLWithState`, preserving the Design section's record-before-callback order. |
| R4-6 | med | plan_v4.md:175 | R3-1 is executable only for autoimport because the CLI and daemon parsers stream bytes without hashing them and `importToJSONLWithStore` returns only an error, so their listed `RecordImport` calls have no parsed-bytes hash unless the builder invents unplanned plumbing or re-hashes the mutable file. | Require both streaming parsers to hash bytes during the read, return or retain that hash through successful import, and add caller-level concurrent-rewrite tests before recording it. |
| R4-7 | med | plan_v4.md:260 | On a fresh log with no matches, `grep -c ... || echo 0` writes two zero lines, so `B0` is not an integer and the literal E2E script fails at its first numeric comparison or arithmetic expansion. | Replace each count assignment with a form that retains `grep -c` output while neutralizing exit status without printing a second zero. |
| R4-8 | med | plan_v4.md:249 | Phase 3 moves `/tmp/bd.new` into `~/.local/bin/bd` before ordering Verification 6-9, so the byte-for-byte E2E script then points at a nonexistent executable. | Run the E2E between build and install or set its literal `BD` path to the installed binary used after step 5. |
| R4-9 | med | plan_v4.md:257 | The E2E starts a daemon but installs no exit trap, so any expected assertion failure exits under `set -e` before `daemon --stop` and leaks the scratch process. | Record the scratch daemon identifier and install an exit trap that stops that exact daemon on both success and failure. |
| R4-10 | med | plan_v4.md:189 | `pgrep -af "bd daemon"` matches the agent shell command containing that literal pattern, contaminating the inventory and making the required empty confirmation fail even after every daemon stops. | Use a self-excluding pattern such as `pgrep -af '[b]d daemon'` for both inventory and the post-stop assertion. |
