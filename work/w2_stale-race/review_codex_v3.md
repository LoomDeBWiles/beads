VERDICT: FIX findings=12
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| R3-1 | high | plan_v3.md:110 | `RecordImport` hashes the file after import instead of recording the hash of the bytes actually parsed, so a concurrent rewrite between the import read and `RecordImport` is blessed as fresh although SQLite contains earlier bytes. | Carry the parsed source hash out of every import, pass it to `RecordImport`, and under the publish lock refuse to commit it unless the current canonical file still has that hash. |
| R3-2 | high | plan_v3.md:95-107 | Git pull, sync-branch copy, and checkout paths do not take the publish lock, so they can change the canonical JSONL after a snapshot callback reads it and have that change silently overwritten by `Publish` rename. | Hold the publish lock across every Beads-controlled canonical-file mutation through its import and `RecordImport`, and make `Publish` abort if the source file changed since the snapshot. |
| R3-3 | high | plan_v3.md:95 | `buildSnapshot` does not require dirty markers and issue data to be read atomically or dirty-first, so a mutation between an issue read and a later marker read can pair old bytes with the new `marked_at` and then be conditionally cleared. | Read the dirty snapshot before any issue data or read both in one SQLite snapshot, then test a mutation during the builder rather than only after it returns. |
| R3-4 | high | plan_v3.md:115-122 | Accepting either hash means a crashed post-rename publish with pending B reports an external restore of old committed A as fresh, so that real file change can remain unimported indefinitely. | Persist rename-coupled pending evidence such as the unique temp pathname and accept old committed content only while that pre-rename temp entry still exists, with the requested old-value restore scenario as a state-machine test. |
| R3-5 | med | plan_v3.md:124,148 | Replacing the CLI import metadata tail unconditionally makes `bd import -i backup.jsonl` record the backup hash as canonical after autoflush publishes the default JSONL, so the next canonical gate reports divergence. | Call `RecordImport` only when the input is the canonical rename destination and let `Publish` own canonical metadata after custom-file imports. |
| R3-6 | med | plan_v3.md:110,147 | `RecordImport` has no failure policy before `onChanged`, so returning on a metadata error can skip the existing full-export callback required after a successful ID-remapping import. | Treat a post-import record failure as a warning and invoke `onChanged` with the original `needsFullExport` value regardless. |
| R3-7 | med | plan_v3.md:124,153-154 | “Resolves to the canonical default JSONL” has no pathname rule, and following the final symlink would misclassify an alias as canonical even though `os.Rename` replaces the alias entry and leaves the default file unchanged. | Compare normalized rename destinations by resolving parent directories but not the final component, and test a relative default path plus a final-component symlink alias. |
| R3-8 | med | plan_v3.md:93,202 | The provisional-mismatch reader uses an uninterruptible blocking flock even though `ContentState` accepts a context, so a wedged publisher can hang direct commands and RPC request goroutines past their timeouts. | Add context-aware publish-lock acquisition and return the context error when the deadline or cancellation fires. |
| R3-9 | med | plan_v3.md:132-143 | Phase 1 still mandates the rejected grep-and-tee baseline and Phase 2 still gates on grep, so a builder following the ordered phases can miss build failures despite the later V2-10 protocol. | Replace both phase commands with the JSON/status/normalizer workflow from Verification and use the artifact-qualified paths in every comparison. |
| R3-10 | med | plan_v3.md:211-213 | The normalizer cannot identify a build/setup failure from a package-level `Action:fail` event alone because Go emits the same event after ordinary test failures, so the specified `pkg` signature can mask a later compile break in an already-failing package. | Emit a distinct `pkg::<build>` signature only when the package has no failed test event or stderr supplies build/setup evidence, and test the normalizer with test-failure and compile-failure fixtures. |
| R3-11 | med | plan_v3.md:215-221 | The accepted V2-11 E2E landing is still prose containing `<id>`, `<scratch db>`, and “bounded wait”, and it uses interactive `$BD init`, so the script is not runnable as written. | Put the literal checked script in the plan using `init --quiet`, parsed ID/PID/log paths, bounded polling commands, exact assertions, and deterministic teardown. |
| R3-12 | med | plan_v3.md:141 | `internal/jsonlpub/jsonlpub_test.go` cannot call the unexported `cmd/bd` function `hasJSONLChanged`, so the required real caller-mapping assertion cannot be implemented in the stated test file. | Keep protocol tests in `internal/jsonlpub`, move `CheckStaleness` mapping tests to `internal/autoimport`, and add `hasJSONLChanged` mapping tests in `cmd/bd`. |
