# Plan Delta v3 for w1_agents-md-guard

Apply to `plan_v2.md` to produce `plan_v3.md`. Every untargeted section is preserved byte-for-byte. Add exactly one new `## What Changed from v2` section immediately after the existing `## What Changed from v1` section, containing a table with columns `Finding`, `Disposition`, `Change`, listing every Finding ID below (F1, F1b, F2, F3, F3b, F4, F5) with a one-line description. The existing `What Changed from v1` section must survive byte-identical.

| Finding ID | Disposition | Heading Path | Prior SHA-256 | Instruction |
|---|---|---|---|---|
| F1 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Design | 7c737fa3f6965605b38775d6595336adc09dc8ce477e376ca3fa1c3b4b7b6288 | Replace the sentence describing the companion factory test with: "a companion `TestFactoryDoesNotTouchAgentsFile` covers every former mutation path: `bd setup factory` with (a) no AGENTS.md, (b) a plain AGENTS.md without markers, (c) an AGENTS.md containing the beads begin/end markers (the update branch); and `bd setup factory --remove` with (d) a marked-only AGENTS.md and (e) a curated AGENTS.md containing a marked section — asserting after every command that the file is absent (a) or byte-identical (b-e)." |
| F1b | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 9f9dc88d3bdacbf241390e8f490c235fdc754a5ed426d169d5f27d244411c58b | In the Phase 1 table, update the `cmd/bd/setup/factory_test.go` row's Change cell to: "`TestFactoryDoesNotTouchAgentsFile` — five subtests: install × {absent, plain, marked} and --remove × {marked-only, curated-marked}, asserting absent or byte-identical after every command". This row's hash binding also authorizes the Phase 1/2/3 edits and gate-title changes in rows F2, F3b, F4 below. |
| F2 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 9f9dc88d3bdacbf241390e8f490c235fdc754a5ed426d169d5f27d244411c58b | (1) Replace the Phase 3 "Pre-deletion gate" paragraph with: "Pre-deletion gate (per file, at execution time, inside the fresh worktree): `sha256sum` of the file must equal one of the two measured generated-template hashes — `bf83e48f58204bea5bee22b987a833b36deedd15253ddafd6bac106a78c1ded2` (the 40-line variant, 12 repos) or `92c1bfa374c21a0f141a06fe8516423d713596c4220f13b7da35eb35a2bb335c` (spec-code's 39-line variant). Any other hash → do not delete; stop and report that repo for manual review (a concurrent curated edit must never be trashed)." (2) In the Phase 3 Executor paragraph, replace "the Edit tool / `git rm`" with "the Edit tool / `trash.sh` + `git add -u`". |
| F3 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Design | 7c737fa3f6965605b38775d6595336adc09dc8ce477e376ca3fa1c3b4b7b6288 | In the list titled "Code to change in `cmd/bd/setup/factory.go`:", rename `SetupFactory` to `InstallFactory` (the actual function name, factory.go:104) in both the list and any surrounding prose of this section, and append a new list item: "`cmd/bd/setup.go` — update the factory command's long help (lines 75-78: 'Creates or updates AGENTS.md...') and the `--remove` flag description (line 122: 'Remove bd section from AGENTS.md') to say the command PRINTS instructions for manual application and never writes AGENTS.md." Also replace both occurrences of the test name `TestSetupFactoryDoesNotTouchAgentsFile` in this section (if present after F1's edit) with `TestFactoryDoesNotTouchAgentsFile`. |
| F3b | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 9f9dc88d3bdacbf241390e8f490c235fdc754a5ed426d169d5f27d244411c58b | In the Phase 1 table: (1) update the `cmd/bd/setup/factory.go` row's Change cell to "InstallFactory prints the integration block instead of writing AGENTS.md; RemoveFactory prints removal instructions instead of editing the file"; (2) add a row: `cmd/bd/setup.go` — "factory command long help + --remove flag description rewritten: command prints instructions, never writes AGENTS.md" — "help must not falsely claim file mutation". (3) In the Phase 1 gate clause and anywhere in this section, replace the test-name pattern `TestInit\|TestSetupFactory` with `TestInit\|TestFactory` and the name `TestSetupFactoryDoesNotTouchAgentsFile` with `TestFactoryDoesNotTouchAgentsFile`. |
| F4 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 9f9dc88d3bdacbf241390e8f490c235fdc754a5ed426d169d5f27d244411c58b | (1) In the Phase 2 table, replace the "Backup current binaries" row's command with: `test -e ~/projects/tools/beads/work/w1_agents-md-guard/backup/MANIFEST.sha256 \|\| { mkdir -p ~/projects/tools/beads/work/w1_agents-md-guard/backup && cp ~/.local/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.local-bin && cp ~/go/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.go-bin && (cd ~/projects/tools/beads/work/w1_agents-md-guard/backup && sha256sum bd.local-bin bd.go-bin > MANIFEST.sha256); }; (cd ~/projects/tools/beads/work/w1_agents-md-guard/backup && sha256sum -c MANIFEST.sha256)` and append to its Why cell: "; the MANIFEST guard makes the backup write-once — a resume after a partial install re-verifies the original copies instead of overwriting them with already-installed binaries". (2) Update the Phase 2 gate clause to "Gate: proof checks 1-5 below pass with pasted output". |
| F5 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Verification | 84a074ce182040c8b5037f1b724557bb5bb4a0051bf63c57744a70aeaa7f645c | (1) Replace proof check #4 with: `command -v bd && bd version && sha256sum ~/.local/bin/bd ~/go/bin/bd && git -C ~/projects/tools/beads rev-parse HEAD` → expected: `/home/ben/.local/bin/bd`; the two binary hashes are EQUAL; the commit embedded in `bd version` is a prefix of the printed merged-main HEAD. (2) Replace proof check #5 with: `d3=$(mktemp -d) && cd $d3 && git init -q && bd init -q && bd setup factory && test ! -e AGENTS.md && echo NOWRITE` → expected: prints the integration block, then `NOWRITE` (own temp repo; independent of checks 2-3). (3) In the Tests line and check #1, replace the run pattern `TestInit\|TestSetupFactory` with `TestInit\|TestFactory` and the test name `TestSetupFactoryDoesNotTouchAgentsFile` with `TestFactoryDoesNotTouchAgentsFile`. |

Materializer notes:
- Do not rename the H1 title. Gate-clause changes to Phase 1 and Phase 2 headings are authorized via the `Changes` bindings above.
- `\|` in instructions denotes a literal pipe character in the produced text.
- Preserve every untargeted section byte-for-byte, including both existing What Changed sections rules: `What Changed from v1` survives byte-identical; add exactly one new `What Changed from v2`.
