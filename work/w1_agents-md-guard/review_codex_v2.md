VERDICT: FIX findings=5
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| F1 | HIGH | work/w1_agents-md-guard/plan_v2.md:76 | The Factory regression test covers only absent and plain existing files for install, so it cannot detect the marked-file update branch or either `RemoveFactory` mutation path, including deletion of a beads-only AGENTS.md. | Exercise `bd setup factory` with absent, plain, and marked files plus `bd setup factory --remove` with marked-only and curated marked files, asserting absence or byte identity after every command. |
| F2 | HIGH | work/w1_agents-md-guard/plan_v2.md:111 | The header, heading, and line-count gate can still trash a concurrently edited curated AGENTS.md that retains those coarse properties, so the accepted full-template hash revalidation is not implemented. | Before each trash operation require the complete file hash to equal one of the two measured generated-template variants and stop on every mismatch. |
| F3 | MED | work/w1_agents-md-guard/plan_v2.md:71 | The plan targets a nonexistent `SetupFactory` instead of `InstallFactory` and omits `cmd/bd/setup.go`, whose command and flag help will still falsely claim that setup and removal mutate AGENTS.md. | Name `InstallFactory` in the implementation steps and update the Factory Cobra help and flag descriptions to describe printed manual instructions. |
| F4 | MED | work/w1_agents-md-guard/plan_v2.md:100 | Fixed backup filenames are overwritten on retry, so a crash after either install step can replace the rollback copies with new binaries and make binary rollback incomplete. | Create one immutable versioned backup set before installation, record and assert its pairwise hashes, and reuse rather than overwrite it on resume. |
| F5 | MED | work/w1_agents-md-guard/plan_v2.md:172 | Proof check 4 never asserts commit or two-copy equality and proof check 5 relies on `$d2` and the working directory inherited from check 3, so the proof set is neither automatic nor independently runnable. | Make check 4 compare both binary hashes and the embedded 12-character commit to merged main, and give check 5 its own temporary repository and expected success marker. |
