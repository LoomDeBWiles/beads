# Plan Delta v2 for w1_agents-md-guard

Apply to `plan_v1.md` to produce `plan_v2.md`. Every untargeted section is preserved byte-for-byte. Heading paths are relative to the H1 root `w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution`; the table's Heading Path column gives the full path. Add exactly one new `## What Changed from v1` section (immediately after the H1 intro blockquote) containing a table with columns `Finding`, `Disposition`, `Change`, listing every Finding ID below with a one-line description of what changed.

| Finding ID | Disposition | Heading Path | Prior SHA-256 | Instruction |
|---|---|---|---|---|
| F1 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Problem | c988176292058ee145fe1d2b3f734702167f8a7d25c54f38bc7fa46736740deb | Append a new final paragraph: "A second writer exists: `bd setup factory` (`cmd/bd/setup/factory.go:105-152`, `SetupFactory`) creates AGENTS.md from a template, appends a marked beads section to an existing file, or rewrites the section between its markers; `RemoveFactory` (factory.go:182+) edits the file to remove that section. Verified by grep across cmd/ and internal/: init.go and setup/factory.go are the only two Go files that write AGENTS.md (setup/cursor.go and setup/aider.go write .cursor/.aider files only). Both writers must be closed or the 'bd never touches AGENTS.md' guarantee is false." |
| F1b | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Design | c233a7170717468a919b3b0d28b17a9cef95167b4786b5de89629eb0378df694 | (1) In the ASCII flow block, add two lines after the `AFTER:` line: "bd setup factory / bd setup factory --remove: BEFORE writes/edits AGENTS.md → AFTER prints the beads integration block (or removal instructions) to stdout for the user to apply manually; CheckFactory stays read-only." (2) After the init.go deletion list, add a short list titled "Code to change in `cmd/bd/setup/factory.go`:" with: `SetupFactory` — replace all three write branches (create/append/update, lines ~105-152) with printing the template block and instructions to stdout, no filesystem writes; `RemoveFactory` — replace file rewrite with printed instructions naming the marker lines to delete; `CheckFactory` unchanged (read-only). (3) Replace the sentence beginning "Regression test follows the existing `TestInitCommand` harness" with: "Regression tests follow the existing `TestInitCommand` harness in `cmd/bd/init_test.go` (t.TempDir + t.Chdir + reset globals + run the command), asserting AGENTS.md filesystem state: `TestInitDoesNotTouchAgentsFile` runs four subtests — {no AGENTS.md, sentinel AGENTS.md} × {default output, --quiet} — asserting the file stays absent / byte-identical; a companion `TestSetupFactoryDoesNotTouchAgentsFile` covers `bd setup factory` for both file states." |
| F1c | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Files NOT Affected (verified) | 3e6f5e72f5b9ba12782d7d29b9ad5adcf6117370ddbefab3e9c155ba0d57222a | Replace the "Other fork Go files" row with two rows: (1) `cmd/bd/setup/cursor.go`, `cmd/bd/setup/aider.go` — Checked: Yes — Why: they write `.cursor` rules and aider config files, never AGENTS.md (only prose mentions); (2) "All other fork Go files" — Checked: Yes — Why: grep across cmd/ and internal/ shows init.go and setup/factory.go are the only AGENTS.md writers, and `updateAgentFile`/`landingThePlaneSection` are referenced only in init.go. |
| F2 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 1, after the "Executed by one Codex builder..." line, add: "Merge step (manager): after the builder's `.done`, pathspec-commit the work-item artifacts on the work branch (`git add work/w1_agents-md-guard/ && git commit -m 'w1: manager artifacts' -- work/w1_agents-md-guard/`), require `git status --porcelain` empty, then `cd ~/projects/tools/beads && wt-merge.sh ~/worktrees/beads/w1_agents-md-guard`. wt-merge refuses dirty worktrees; nothing may remain uncommitted." Also apply the Phase 1/2/3 edits from rows F1d, F3, F4, F5, F6, F7, F8, F9 below — this row's hash binding authorizes all subsection restructuring under Changes, including gate-title changes. |
| F1d | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In the Phase 1 table add two rows: `cmd/bd/setup/factory.go` — "SetupFactory prints the integration block instead of writing AGENTS.md; RemoveFactory prints removal instructions instead of editing the file" — "close the second AGENTS.md writer"; `cmd/bd/setup/factory_test.go` (new file or extend existing setup tests) — "TestSetupFactoryDoesNotTouchAgentsFile: both file states" — "pins the no-write behavior". Update the Phase 1 gate to: "Gate: `go build ./...` clean and `go test ./cmd/bd/... -run 'TestInit|TestSetupFactory' -v` passes, including the new no-touch tests". |
| F3 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 2, replace the "Backup current binaries" row's command with: `mkdir -p ~/projects/tools/beads/work/w1_agents-md-guard/backup && cp ~/.local/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.local-bin && cp ~/go/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.go-bin && sha256sum ~/.local/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.local-bin ~/go/bin/bd ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.go-bin` and add to its Why cell: "persistent primary path that exists after the worktree is merged and removed; backups stay untracked (never committed — 33 MB each); hashes prove the copies are intact". |
| F4 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In the Phase 3 repo table, replace "git rm AGENTS.md (wholly bd-generated template)" with: "`~/projects/shared-docs/scripts/trash.sh <worktree>/AGENTS.md` then `git add -u -- AGENTS.md` and pathspec-commit (wholly bd-generated template; trash gives a 14-day undo on top of git history)". |
| F5 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 3, after the per-repo procedure line, add: "Pre-deletion gate (per file, at execution time, inside the fresh worktree): the file must satisfy all of — first line is exactly `# Agent Instructions`, contains `## Landing the Plane`, and is ≤41 lines. Any mismatch → do not delete; stop and report that repo for manual review (classification is from planning time and could be stale)." |
| F6 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 3: (1) reword the repo list entry `teaching/548-2026/website` to `teaching (file: 548-2026/website/AGENTS.md)` and add a note after the table: "teaching/548-2026/website is not its own repo — `git rev-parse --show-toplevel` = `~/projects/teaching`; the worktree is created for repo `teaching` and the target path inside it is `548-2026/website/AGENTS.md`." (2) The per-repo procedure line must name repo roots, e.g. `wt-new.sh <repo-root-name> --next strip-bd-boilerplate`. |
| F7 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 3's per-repo procedure line, delete the parenthetical about `--no-push`; replace with: "(repos without an origin remote — fleet, tts-reader, teaching — need no flag: `wt-merge.sh` skips the push automatically when no origin exists and still completes merge + cleanup; `--no-push` must NOT be used, it leaves the worktree and branch in place)". |
| F8 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | Replace the Phase 3 heading's gate clause with: "Gate: fleet-wide re-sweep `find /home/ben/projects -name AGENTS.md -not -path '*/.git/*' 2>/dev/null | xargs grep -l -e 'Landing the Plane' -e 'NEVER stop before pushing'` returns exactly `tools/beads/AGENTS.md` and `tools/beads/cmd/bd/AGENTS.md`. Traversal stderr from unreadable directories is suppressed and safe to ignore: bd runs as user ben, so bd-written pollution cannot live in a directory ben cannot read. The second pattern is a stable body phrase catching a copy whose heading was edited." |
| F9 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Changes | 0bb79550ad26e9a2d9d4fa13b32199d77ab2c973f7cef4294266f0b233f0a74e | In Phase 1's table, update the `cmd/bd/init_test.go` row's Change cell to: "Add `TestInitDoesNotTouchAgentsFile` — four subtests: {no AGENTS.md, sentinel AGENTS.md} × {default, --quiet}". In Phase 2's E2E row, state that the scratch runs use DEFAULT (non-quiet) `bd init` in one repo and `bd init -q` in the other, so both output modes are exercised against the installed binary. |
| F10 | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Verification | ff06df4abbd594ac4d06eb9249e938895fdcc2f222345644c4bb03ec2022cdee | Rewrite the section: (1) Tests line becomes `cd ~/worktrees/beads/w1_agents-md-guard && go build ./... && go test ./cmd/bd/... -run 'TestInit|TestSetupFactory' -v`. (2) Keep the E2E paragraph, adding that one scratch repo uses default output and the other `-q`. (3) Replace the proof-of-behavior table with exact runnable checks: #1 `go test ./cmd/bd/... -run 'TestInit|TestSetupFactory' -v` → all PASS incl. TestInitDoesNotTouchAgentsFile + TestSetupFactoryDoesNotTouchAgentsFile; #2 `d=$(mktemp -d) && cd $d && git init -q && printf 'SENTINEL-w1\n' > AGENTS.md && cp AGENTS.md /tmp/sentinel.ref && bd init && cmp AGENTS.md /tmp/sentinel.ref && echo UNCHANGED` → `UNCHANGED`; #3 `d2=$(mktemp -d) && cd $d2 && git init -q && bd init -q && test ! -e AGENTS.md && echo ABSENT` → `ABSENT`; #4 `command -v bd && bd version && git -C ~/projects/tools/beads rev-parse --short HEAD` → `/home/ben/.local/bin/bd`, and the version's embedded commit equals the printed merged-main SHA; #5 `bd setup factory && test ! -e AGENTS.md && echo NOWRITE` (run inside $d2) → prints integration block, `NOWRITE`; #6 the Phase 3 gate sweep command → exactly the two tools/beads paths. |
| F3b | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Rollback | 18ea9e7662e13371f6485fb2033ef7cf51cc60dac01bba3b20108dea63923c6e | Replace the Binaries bullet with: "Binaries: `install -m755 ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.local-bin ~/.local/bin/bd && install -m755 ~/projects/tools/beads/work/w1_agents-md-guard/backup/bd.go-bin ~/go/bin/bd` (backups verified by sha256 at Phase 2)." |
| F7b | ACCEPT | w1_agents-md-guard: Stop `bd init` from writing AGENTS.md, and undo the existing pollution > Risks | e1736f274e1f9682b9cf7679f3cf314a7c7efd2b4e9d715497a4e9ee6976954a | Replace the "wt-merge push fails on the 3 remote-less repos" row with: "Risk: assuming remote-less repos need special flags — Mitigation: wt-merge skips push when no origin exists and completes cleanup; no flag passed (verified at wt-merge.sh push_primary)." Add row: "Risk: `bd setup factory` behavior change surprises a future Factory Droid setup — Mitigation: command still prints the exact block to paste; no silent loss, and no-touch test documents the contract." |

Materializer notes:
- Do not rename the H1 title or any heading other than the Phase 1 and Phase 3 gate clauses (authorized via the `Changes` bindings above).
- Preserve every untargeted section byte-for-byte.
- The `What Changed from v1` section lists all 15 finding IDs above (F1, F1b, F1c, F1d, F2, F3, F3b, F4, F5, F6, F7, F7b, F8, F9, F10).
