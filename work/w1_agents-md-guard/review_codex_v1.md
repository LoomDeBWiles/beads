VERDICT: FIX findings=10
| ID | sev | file:line | defect (one sentence) | fix (one sentence) |
|---|---|---|---|---|
| F1 | HIGH | cmd/bd/setup/factory.go:103 | The promised end state that bd never creates or modifies AGENTS.md is false because `bd setup factory` still creates, appends, rewrites, and removes that file. | Make the Factory setup command emit integration instructions without mutating AGENTS.md and add no-touch tests for missing and existing files. |
| F2 | HIGH | work/w1_agents-md-guard/plan_v1.md:61 | Phase 1 cannot reach the required merge because this worktree contains untracked `work/` artifacts and `wt-merge.sh` refuses any dirty worktree. | Preserve the manager and review artifacts at a stable owner path outside the worktree, then require an empty `git status --porcelain` before merging. |
| F3 | HIGH | work/w1_agents-md-guard/plan_v1.md:67 | The binary backup command targets a literal nonexistent `{workdir}/backup`, can mask the first failed copy because it uses `;`, and points into a worktree that Phase 2 says has already been removed. | Create a concrete persistent backup directory outside the worktree, join both copies with `&&`, and verify both backup hashes before installation. |
| F4 | HIGH | work/w1_agents-md-guard/plan_v1.md:80 | The 13 file deletions use `git rm` despite the governing trash-only deletion rule, so the cleanup procedure is not runnable under the required safety rail. | Move each exact worktree file with `trash.sh`, then stage its deletion with `git add -u -- <repo-relative-path>`. |
| F5 | HIGH | work/w1_agents-md-guard/plan_v1.md:49 | The plan deletes 13 files based only on planning-time classification, so a concurrent curated edit made before execution would be lost without any failing gate. | Immediately before each deletion, verify the file still matches the measured generated-template hash and stop for review on any mismatch. |
| F6 | MED | work/w1_agents-md-guard/plan_v1.md:80 | Teaching is rooted at `/home/ben/projects/teaching`, where the polluted tracked path is `548-2026/website/AGENTS.md`, so the listed `git rm AGENTS.md` targets the wrong path. | Use `548-2026/website/AGENTS.md` as the teaching worktree-relative path in the edit, commit, and rollback commands. |
| F7 | MED | work/w1_agents-md-guard/plan_v1.md:76 | Passing `--no-push` for the three repositories without an origin causes `wt-merge.sh` to leave each worktree and branch in place even though the script already handles a missing origin. | Omit `--no-push` for those repositories so `wt-merge.sh` merges locally and completes its normal cleanup. |
| F8 | MED | work/w1_agents-md-guard/plan_v1.md:74 | The fleet gate currently exits 2 on unreadable `farmplanner/frontend/redis-data` and searches only the exact heading, so it cannot prove a complete scan or detect altered boilerplate. | Enumerate tracked and untracked AGENTS.md files per Git repository with traversal errors fatal, then compare both heading and stable body signatures against an explicit allowlist. |
| F9 | MED | work/w1_agents-md-guard/plan_v1.md:33 | The regression design requires only the two filesystem states and the installed-binary E2E uses only `-q`, so a future mutation limited to default non-quiet init can pass every required check. | Run both missing-file and sentinel-file assertions under default init and `--quiet`, producing four explicit subtests and E2E checks. |
| F10 | MED | work/w1_agents-md-guard/plan_v1.md:131 | Proof checks 2 through 4 are not exact runnable checks because `scratch:` and `scratch2:` are prose placeholders, isolation is unspecified, and check 4 neither expects the absolute PATH nor compares a concrete commit. | Provide complete temporary-directory commands using `cmp` and `test ! -e`, and compare `command -v bd` plus the version commit directly with the merged main SHA. |
